require 'spec_helper'

describe ErlangConfigParser do
  it 'has a version number' do
    expect(ErlangConfigParser::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
